#ifndef CONSTANTS_H
#define CONSTANTS_H
#include <gst/gst.h>

const guint32 MAX_STEADY_BITRATE = 6000;
const guint32 MIN_STEADY_BITRATE = 500;
const guint32 INC_STEADY_BITRATE = 750;
const guint32 DEC_STEADY_BITRATE = 1000;

const guint32 MAX_CONGESTION_BITRATE = 1000;
const guint32 MIN_CONGESTION_BITRATE = 100;
const guint32 INC_CONGESTION_BITRATE = 125;
const guint32 DEC_CONGESTION_BITRATE = 500;

const guint32 LOW_QUAL_BITRATE = 500;
const guint32 MED_QUAL_BITRATE = 1000;
const guint32 HIGH_QUAL_BITRATE = 3000;

#endif